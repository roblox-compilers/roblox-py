--// Compiled using roblox-pyc \--
		
		
------------------------------------ BUILT IN -------------------------------
local py, libs, builtin = unpack(require(game.ReplicatedStorage["roblox.pyc"])(script).py)

local stringmeta = builtin.stringmeta
local list = builtin.list
local dict = builtin.dict
local python = builtin.python
local staticmethod = builtin.staticmethod
local classsmethod = builtin.classsmethod
local class = builtin.class
local range = builtin.range
local __name__ = builtin.__name__
local len = builtin.len
local abs = builtin.abs
local str = builtin.str
local int = builtin.int
local sum = builtin.sum
local max = builtin.max
local min = builtin.min
local reversed = builtin.reversed
local split = builtin.split
local round = builtin.round
local all = builtin.all
local any = builtin.any
local ord = builtin.ord
local char = builtin.char
local callable = builtin.callable
local zip = builtin.zip
local float = builtin.float
local format = builtin.format
local hex = builtin.hex
local id = builtin.id
local map = builtin.map
local bool = builtin.bool
local divmod = builtin.divmod
local slice = builtin.slice
local operator_in = builtin.operator_in
local asynchronousfunction = builtin.asynchronousfunction
local match = builtin.match
local anext = builtin.anext
local ascii = builtin.ascii
local dir = builtin.dir
local getattr = builtin.getattr
local globals = builtin.globals
local hasattr = builtin.hasattr
local input = builtin.input
local isinstance = builtin.isinstance
local issubclass = builtin.issubclass
local iter = builtin.iter
local locals = builtin.locals
local oct = builtin.oct
local open = builtin.open
local ord = builtin.ord
local pow = builtin.pow
local eval = builtin.eval
local exec = builtin.exec
local filter = builtin.filter
local frozenset = builtin.frozenset
local aiter = builtin.aiter
local bin = builtin.bin
local complex = builtin.complex
local delattr = builtin.delattr
local enumerate = builtin.enumerate
local breakpoint = builtin.breakpoint
local bytearray = builtin.bytearray
local bytes = builtin.bytes
local compile = builtin.compile
local help = builtin.help
local memoryview = builtin.memoryview
local repr = builtin.repr
local sorted = builtin.sorted
local vars = builtin.vars
local __import_ = builtin.__import_
local formatmod = builtin.formatmod

-----------------------------------------------------------------------------

